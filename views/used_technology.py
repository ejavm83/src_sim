"""이 앱에 쓰인 핵심 기술(SimPy, OR-Tools CP-SAT, Streamlit) — 비전문가용 설명."""

from __future__ import annotations

from pathlib import Path

import streamlit as st
import streamlit.components.v1 as components

_DIAGRAMS_DIR = Path(__file__).resolve().parent / "diagrams"


def _show_diagram(svg_name: str, *, height: int = 420) -> None:
    """미리 생성된 Mermaid SVG를 표시한다(iframe JS 렌더링 대신 정적 SVG 사용)."""
    svg_path = _DIAGRAMS_DIR / svg_name
    svg = svg_path.read_text(encoding="utf-8")
    components.html(
        f"""
        <div style="width:100%;overflow-x:auto;line-height:0">
            {svg}
        </div>
        """,
        height=height,
        scrolling=True,
    )


def _render_overall_flowchart() -> None:
    st.markdown("##### 📊 전체 순서도")
    _show_diagram("overall_flow.svg", height=560)
    st.caption(
        "파란색 = 핵심 계산(SimPy) · 보라색 = AI 추출(Gemini) · 주황색 = 사후 비교(CP-SAT). "
        "화면(탭·버튼·차트)은 모두 Streamlit이 담당합니다."
    )


def _render_simpy_process_flowchart() -> None:
    st.markdown("##### 📊 SimPy 공정 흐름 순서도")
    _show_diagram("simpy_process.svg", height=320)
    st.caption("실선 화살표 = 물류·재료 흐름. 점선 = SimPy가 모델링하는 설비·대기열.")


def _render_simpy_cpsat_sequence() -> None:
    st.markdown("##### 📊 SimPy ↔ CP-SAT 순서도")
    _show_diagram("simpy_cpsat.svg", height=480)
    st.caption("CP-SAT는 SimPy가 끝난 뒤, 고급 분석을 열 때만 실행됩니다. SimPy 결과를 바꾸지 않습니다.")


def render() -> None:
    st.header("📘 이 앱에서 사용된 기술")
    st.caption(
        "구리 스크랩 공정(입고 → 선별·압착 → 용해 → 주조 → 출하)을 컴퓨터 안에서 돌려 보고, "
        "병목·생산량·일정 여유를 분석하는 도구입니다. 아래는 **어떤 기술이 언제, 무엇을 하는지**를 "
        "일반인도 따라갈 수 있게 정리한 설명입니다."
    )

    st.subheader("0) 전체 흐름 — 버튼을 누르면 무슨 일이 일어나나요?")
    _render_overall_flowchart()
    st.markdown(
        """
**비유:** 공장 설명서를 읽고 → 숫자만 뽑아 레고 설명서 조각 수로 바꾼 뒤 → 컴퓨터 안 가상 공장을 며칠치 돌려 보고 → “어디가 막혔는지” 보고서를 보여 주는 과정입니다.

| 단계 | 화면·동작 | 쓰이는 기술 |
|------|-----------|-------------|
| ① | **📄 공정 설명** 탭에서 한국어 문서(MD) 보기·편집·저장 | Streamlit |
| ② | **「공정 트리로 추출」** — 문서를 공정 트리로 분해하고 숫자를 시뮬 설정으로 변환 | Gemini (AI) |
| ③ | 사이드바에서 파라미터 확인·조정 (엑셀 기본값과 병행 가능) | Streamlit |
| ④ | **🚀 시뮬레이션 실행** — 가상 공장을 설정 기간(기본 7일)만큼 돌림 | **SimPy** ⭐ |
| ⑤ | KPI·병목·버퍼·일별 생산·인사이트 표시 | Streamlit + 분석 로직 |
| ⑥ | (선택) **고급 분석** — 반사로 순서의 이론적 최적과 비교 | **CP-SAT** |

**📄 공정 설명** 탭에서 문서를 고칠 때는 사람이 직접 텍스트를 수정합니다. AI가 대화로 문장을 바꿔 주지는 않고, **추출** 단계에서만 문서를 읽어 숫자를 뽑습니다.

**🆚 스냅샷 비교** 탭에서는 실행할 때마다 자동 저장된 결과끼리 KPI·설정·추이를 나란히 볼 수 있습니다.
"""
    )

    st.divider()
    st.subheader("1) Streamlit — 지금 보고 계신 **웹 대시보드**")
    st.markdown(
        """
**한 줄로:** Python 코드로 **버튼·그래프·표·탭**이 있는 화면을 빠르게 만들어, **브라우저**로 여는 방식입니다.

**비유:**  
- 엑셀 매크로로 표를 만드는 것과 비슷하게, **“코드로 화면을 그린다”**고 생각하시면 됩니다.  
- 사용자가 숫자를 바꾸고 실행을 누르면, 서버에서 다시 계산하고 **화면을 갱신**합니다.

**이 앱에서 하는 일:**  
사이드바의 시뮬 조건, 시뮬레이션 실행 버튼, 결과 차트, 공정 설명 편집기 등 **대부분의 화면**이 Streamlit 기반입니다.  
차트·표 쪽은 **Plotly**, 데이터 가공은 **Pandas**를 함께 씁니다.

**일반인에게 중요한 점:**  
Streamlit 자체가 “공장을 예측한다”기보다, **SimPy가 낸 결과를 보기 좋게 보여 주고**, 버튼으로 **다시 돌려 보게** 해 주는 **창문**에 가깝습니다.
"""
    )

    st.divider()
    st.subheader("2) Gemini (Google AI) — 자연어 문서에서 **숫자만** 뽑기")
    st.markdown(
        """
**한 줄로:** **📄 공정 설명** 문서에 적힌 “하루 트럭 10대”, “용해 약 13시간” 같은 문장을 읽고, 시뮬레이션에 넣을 **숫자 목록**으로 바꿔 주는 도구입니다.

**비유:**  
- 긴 공장 설명서를 사람이 일일이 표에 옮기는 대신, **숫자만 골라 표로 정리해 주는 비서**에 가깝습니다.  
- 문서에 **없는 값은 추측하지 않습니다.** 없으면 기본값(엑셀·코드)을 그대로 둡니다.

**이 앱에서 하는 일:**  
1. **「공정 트리로 추출」** 버튼을 누르면 Gemini가 문서를 **배치 → 단계 → 작업 → 파라미터/특성** 트리로 분해합니다.
2. 트럭 수, 하역 시간, 반사로 대수, 용해 시간 등 **40여 개 항목**을 표준 필드에 매핑해 뽑습니다.
3. **「시뮬레이션에 적용」**을 누르면 현재 기본값(또는 마지막 적용값)과 **다른 항목만** 표로 보여 줍니다.
4. **📊 파라메터** 탭에서 **적용**하면 왼쪽 사이드바 파라미터에 반영됩니다.

**API 키:** **⚙️ 설정** 탭에서 등록하거나, 환경 변수 `GEMINI_API_KEY`로 넣을 수 있습니다.

**주의:** LLM은 **시뮬레이션을 돌리지 않습니다.** 숫자 추출·정리만 담당하고, 실제 가상 공장 계산은 **SimPy**가 합니다.
"""
    )

    st.divider()
    st.subheader("3) SimPy — “가상 공장 시계”를 돌리는 **핵심 엔진**")
    st.markdown(
        """
**한 줄로:** 실제 공장을 세우지 않고, 컴퓨터 안에서 **트럭·설비·대기 줄이 시간 순서대로 움직이는 장난감 세트**처럼 재현하는 도구입니다.

**비유:**  
- 각 트럭·반사로·압착기는 **각자 할 일이 있는 사람**처럼 따로 움직입니다.  
- 계근대는 **한 번에 한 대만 올라갈 수 있는 다리** 같고, 버퍼(창고)는 **자리가 정해진 대기열** 같습니다.  
- SimPy는 “지금 몇 시인지”, “누가 누구를 기다리는지”를 **분 단위로 계속 밀어 나가며** 기록합니다.

**이 앱에서 모델링하는 5단계:**  
입고/하역 → 선별/압착 → 장입/용해 → 하이브리드 주조 → 출하

"""
    )
    _render_simpy_process_flowchart()
    st.markdown(
        """
| 현실 | SimPy 표현 |
|------|------------|
| 계근대, 하역베이, 압착기, 반사로 등 | `Resource` — 한 번에 N대만 사용 가능 |
| 선별 대기열, 파레트 버퍼, 야적장 | `Store` — 정해진 용량의 창고 |

**SimPy가 언제, 무엇을 하나요?**

| 시점 | 동작 |
|------|------|
| **🚀 시뮬레이션 실행** 직후 | 가상 시계를 만들고, 설비·대기열을 준비합니다. |
| **시작 직후** | 입고·선별·압착·용해·주조·출하 **작업자 프로세스**를 동시에 가동합니다. |
| **시뮬 중** | 트럭 도착(확률적), 설비 점유, 줄 서기, 버퍼 적재/소진을 처리합니다. |
| **반사로 배정** | **선착순(FIFO)** — 먼저 온 배치가 먼저 들어갑니다. |
| **종료** | 설정 기간(기본 7일)까지 진행 후, 이벤트·생산량·가동 시간을 수집합니다. |

**조금만 더 깊게**

- **이산사건**이란 “매 초마다”가 아니라, **‘트럭이 도착했다’ ‘용해가 끝났다’**처럼 **의미 있는 순간**을 중심으로 시계를 진행한다는 뜻에 가깝게 이해하시면 됩니다.
- SimPy는 **웹 화면을 만드는 프로그램이 아니라**, “뒤에서 숫자를 굴리는” **시뮬레이션 계산 엔진**입니다. 이 앱에서 **가장 중요한 계산**은 모두 SimPy가 담당합니다.
"""
    )

    st.divider()
    st.subheader("4) OR-Tools CP-SAT — 반사로 **순서·일정**의 이론적 비교")
    st.markdown(
        """
**한 줄로:** “반사로에 들어갈 작업 순서를 **서로 겹치지 않게** 짤 때, 이론상 **전체가 가장 빨리 끝나는 쪽**에 얼마나 가까운지”를 **별도로** 살펴볼 때 쓰는 **최적화(조합 찾기) 도구**입니다.

> **이름 참고:** 코드·문서에는 **CA-SAT**가 아니라 **CP-SAT**로 표기합니다. **CP**(제약 프로그래밍) + **SAT**(조건을 만족하는 배치 찾기) 계열의 Google OR-Tools 솔버입니다.

**비유:**  
- SimPy가 **실제 규칙(줄 서기, 설비 점유, 버퍼)**으로 돌린 **현실적인 결과**라면,  
- CP-SAT는 같은 조건을 **퍼즐처럼 풀어** “순서만 바꿔도 **최대한 빨리 끝낼 수 있는 한계**가 어디쯤인가?”를 **비교용 답**으로 보여 주는 계산기에 가깝습니다.

**SimPy vs CP-SAT**

| | SimPy | CP-SAT |
|---|-------|--------|
| 범위 | 입고~출하 **전체** 5단계 | **반사로 일정만** |
| 방식 | 줄 서기·버퍼·확률 반영 | 순서만 최적으로 짜는 수학 퍼즐 |
| 역할 | “이 규칙으로 돌리면?” | “순서만 바꾸면 이론상 얼마나 더 빠를까?” |

"""
    )
    _render_simpy_cpsat_sequence()
    st.markdown(
        """
**CP-SAT가 언제 동작하나요?**

1. **먼저 SimPy**가 시뮬레이션을 끝내고, 각 배치의 시작·끝 시각을 기록합니다.  
2. 결과 화면에서 **「🔬 고급 분석」**을 열면 CP-SAT가 실행됩니다.  
3. “반사로 N기에 배치를 겹치지 않게 넣었을 때 **최소 총 소요시간(makespan)**”을 계산합니다(최대 약 5초).  
4. **SimPy FIFO 결과**와 **CP-SAT 이론 최적**을 나란히 비교·간트 차트로 보여 줍니다.

**중요:** CP-SAT는 SimPy 결과를 **바꾸지 않습니다.** 사후 **비교·통찰용**이며, 공장 전체를 CP-SAT로 대체하지도 않습니다.
"""
    )

    st.divider()
    st.subheader("5) 사용 시나리오 예시")
    st.markdown(
        """
1. **📄 공정 설명**에서 “반사로 3기, 용해 12시간”처럼 문서를 수정하고 저장합니다.  
2. **공정 트리로 추출** → **시뮬레이션에 적용** → **📊 파라메터** 탭에서 변경 항목 확인 후 **적용**합니다.
3. 사이드바에서 세부 조정 후 **🚀 시뮬레이션 실행**합니다. (**SimPy**가 7일치 가상 공장 운영)  
4. **🏭 시뮬레이션** 탭에서 생산량·병목·버퍼를 확인합니다.  
5. (선택) **고급 분석**에서 **CP-SAT**로 반사로 순서 최적화 여지를 봅니다.  
6. 설정을 바꿔 다시 돌리고 **🆚 스냅샷 비교**로 전후 차이를 봅니다.
"""
    )

    st.info(
        "**정리:** Streamlit = 화면 · Gemini = 문서에서 숫자 추출 · "
        "SimPy = 가상 공장 돌리기(핵심) · CP-SAT = 반사로 순서의 이론적 비교(사후)."
    )
